// const n = 6;
const array = [0, 0, 0, 0, 1, 0];


function jumpingOnClouds(c) {
  // count = 0
  // current cloud = index 0
  // iterate through array
  // if [i+2] = 0, then current cloud index = [i+2], count ++
  // if [i+1] = 0, then current cloud index = [i+1], count ++
  // if both = 1, you lost
  // return count
}

console.log(jumpingOnClouds(array))